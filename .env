# Default env. Real secrets live in .env.local (gitignored).
# Copy from .env.example to set up new environments.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
USE_MEMORY_DB=true
STORAGE_DRIVER=local
STORAGE_LOCAL_PATH=./.uploads
